import { FormularioRecuperar } from "@/components/formularios-acceso";

export const metadata = { title: "Recuperar contraseña" };

export default function Pagina() {
  return <FormularioRecuperar />;
}
